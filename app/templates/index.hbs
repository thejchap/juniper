<div class="container-fluid">
  {{#masonry-grid columnWidth='.grid-sizer'}}
    <div class="grid-sizer"></div>
    {{#each model as |stem|}}
      {{sprh-bulb stem=stem}}
    {{/each}}
  {{/masonry-grid}}
</div>
